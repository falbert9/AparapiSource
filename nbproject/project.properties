annotation.processing.enabled=true
annotation.processing.enabled.in.editor=false
annotation.processing.processor.options=
annotation.processing.processors.list=
annotation.processing.run.all.processors=true
annotation.processing.source.output=${build.generated.sources.dir}/ap-source-output
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
buildfile=nbbuild.xml
# Uncomment to specify the preferred debugger connection transport:
#debug.transport=dt_socket
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# Files in build.classes.dir which should be excluded from distribution jar
dist.archive.excludes=
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/Aparapi.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.add-src=samples/add/src
file.reference.blackscholes-src=samples/blackscholes/src
file.reference.convolution-src=samples/convolution/src
file.reference.EdgeFilter-src=samples/EdgeFilter/src
file.reference.effects-src=examples/effects/src
file.reference.extension-src=samples/extension/src
file.reference.info-src=samples/info/src
file.reference.javaonedemo-src=examples/javaonedemo/src
file.reference.life-src=samples/life/src
file.reference.mandel-src=samples/mandel/src
file.reference.MatMul-src=samples/MatMul/src
file.reference.mdarray-src=samples/mdarray/src
file.reference.MMul-src=samples/MMul/src
file.reference.movie-src=examples/movie/src
file.reference.nbody-src=examples/nbody/src
file.reference.oopnbody-src=examples/oopnbody/src
file.reference.squares-src=samples/squares/src
file.reference.src-java=com.amd.aparapi/src/java
file.reference.src-java-1=test/codegen/src/java
file.reference.src-java-2=test/runtime/src/java
includes=**
jar.compress=false
java.dir=${file.reference.src-java}
javac.classpath=
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.processorpath=\
    ${javac.classpath}
javac.source=1.7
javac.target=1.7
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javac.test.processorpath=\
    ${javac.test.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
mkdist.disabled=false
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project.
# You may also define separate properties like run-sys-prop.name=value instead of -Dname=value.
# To set system properties for unit tests define test-sys-prop.name=value:
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=${file.reference.effects-src}
src10.dir=${file.reference.squares-src}
src11.dir=${file.reference.blackscholes-src}
src12.dir=${file.reference.info-src}
src13.dir=${file.reference.EdgeFilter-src}
src14.dir=${file.reference.javaonedemo-src}
src15.dir=${file.reference.movie-src}
src16.dir=${file.reference.nbody-src}
src17.dir=${file.reference.extension-src}
src2.dir=${file.reference.convolution-src}
src3.dir=${file.reference.add-src}
src4.dir=${file.reference.oopnbody-src}
src5.dir=${file.reference.MMul-src}
src6.dir=${file.reference.mandel-src}
src7.dir=${file.reference.MatMul-src}
src8.dir=${file.reference.life-src}
src9.dir=${file.reference.mdarray-src}
test.java.dir=${file.reference.src-java-2}
test.src.dir=${file.reference.src-java-1}
